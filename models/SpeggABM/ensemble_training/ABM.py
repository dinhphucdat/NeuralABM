from . import *

def simulate_population(params : Iterable):
    """
    Simulate the population with the given parameters.

    Args:
        params (Iterable): An iterable of parameters to be passed to the simulation.

    Returns:
        The result of the simulation.
    """
    return simulate(params)